{% set name = "tensorflowonspark" %}
{% set version = "2.2.0" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  url: "https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz"
  sha256: b3c1216deee12581dda235aa96b40550b3f45013d8565a16e9c42675529c05ce

build:
  number: 0
  script: "{{ PYTHON }} -m pip install . -vv"
  noarch: python

requirements:
  host:
    - pip
    - python
  run:
    - python

test:
  imports:
    - tensorflowonspark

about:
  home: "https://github.com/yahoo/TensorFlowOnSpark"
  license: Apache-2.0
  license_family: APACHE
  license_file: LICENSE
  summary: "Deep learning with TensorFlow on Apache Spark clusters"
  doc_url: https://yahoo.github.io/TensorFlowOnSpark

extra:
  recipe-maintainers:
    - afloren
